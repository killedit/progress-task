# versioning is deprecated in docker compose
services:
  laravel:
    build:
      context: .
      dockerfile: ./laravel/Dockerfile
    container_name: progress_laravel
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./laravel:/var/www/html
    env_file:
      - ./laravel/.env
    networks:
      - progress_net
    depends_on:
      - mysql

  nginx:
    image: nginx:alpine
    container_name: progress_nginx
    restart: unless-stopped
    ports:
      - "8007:80"
    volumes:
      - ./laravel:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - progress_net
    depends_on:
      - laravel

  vuejs:
    build: ./vuejs
    container_name: progress_vuejs
    restart: unless-stopped
    ports:
      - "8087:80" # Vue app served by Nginx
    volumes:
      - ./vuejs:/app # only needed for live-editing
    networks:
      - progress_net
    depends_on:
      - nginx

  mysql:
    image: mysql:8.0
    container_name: progress_mysql
    restart: unless-stopped
    env_file:
      - ./.env
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - progress_net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  progress_net:

volumes:
  mysql_data:
